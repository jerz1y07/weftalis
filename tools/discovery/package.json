{
  "name": "weft-place-discovery",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "External-first Workflow discovery for Weft Place",
  "engines": {
    "node": "^20.19.0 || ^22.12.0 || >=24.0.0"
  },
  "scripts": {
    "discover": "node --import tsx src/cli.ts",
    "test": "vitest run --dir tests",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.23.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
